/*© Michael Karssen 99038898*/
/*Applicatie en media ontwikkelaar*/

function AddToOrder(){
	var bestelling = prompt('Wat wilt u bestellen?');
		if(bestelling === 'fris' || bestelling === 'bier' || bestelling === 'wijn'){
				parseInt(prompt('Hoeveel ' + bestelling + ' wilt u bestellen?'));
			if (bestelling === 'fris') {
				var AmountFris = AmountFris + Amount;
			}
		}
}
